# SupaCell 需求文档

SupaCell 是一个 Web 项目，它看上去简洁、优雅，页面功能清晰，易于理解和使用。

**定位**：内部使用的临时数据操作工具，页面刷新即丢失数据，无需持久化存储。

## 核心功能

核心功能是一个可编辑的在线表格，默认表格有 5 列，分别是：

- ID
- Name
- Gender
- content
- created_at

在所有列的最后，有一个"+"列（仅有表头，没有数据），点击它会弹出一个模态框，模态框中包含以下表单：

- 字段名称
- 字段类型
    - 文本
        - 默认值
    - 数值
        - 默认值
    - 日期时间
        - 默认值
    - 单选
        - 选项
    - 多选
        - 选项
    - AI
        - 模型
            - DeepSeek3.2
            - Qwen3.5 Plus
        - prompt
            - prompt 的输入框是一个 textarea，其右上角有一个"引用字段"按钮，点击它会弹出一个模态框，模态框中包含一个字段列表（列表顶部有一个搜索框，用户可以搜索字段），用户可以选择字段，选择之后，该字段的引用会自动添加到 prompt 中（使用 `{{field_name}}` 语法）

用户可以添加、删除、修改表格中的数据，也可以修改表格的列，包括列名称、列类型、列顺序等。

与一般可编辑在线表格不同的是，用户在 SupaCell 中新增一个列的时候，除了选择常规的字段类型（数值、文本、日期时间、单选、多选）之外，还可以选择**AI 字段类型**。

AI 字段类型的数据，是通过调用 AI 模型来生成的，用户可以设置 AI 模型的参数：

- AI 模型（可选 DeepSeek3.2 或 Qwen3.5 Plus）
- AI 模型的输入（prompt）
- 生成模式
    - 生成 [N] 条（默认 N=10，数字可更改）
    - 生成整列

## 技术栈

- **框架**：Next.js（单一技术栈，前后端一体化）
- **UI 和 表格组件选型推荐**：
    - 表格核心库：建议使用 `@tanstack/react-table` (Headless UI) 或自带虚拟滚动的复合型库 (如 `ag-grid-community`、`Glide Data Grid`)。
    - 虚拟滚动库：如果采用自建 UI 结合 @tanstack/react-table，建议搭配 `@tanstack/react-virtual` 或 `react-virtualized` 来实现 DOM 回收。
- **数据存储**：仅在内存中保存，页面刷新即丢失，无需数据库
- **前端渲染性能**：必须引入虚拟滚动（Virtual Scrolling），以确保在 1000 条数据、多列展示且无分页的场景下，大量 DOM 节点和复杂的交互组件不会带来性能问题，保证极致流畅的操作体验。
- **AI 调用**：后端 API 调用，API Key 配置在服务器 `.env` 环境中
- **状态管理**：React 状态管理（如 Zustand 或 React Context）

## 页面布局

### 顶部栏

顶部栏包含以下内容：

- 项目名称（SupaCell）
- 设置入口（齿轮图标 + 文字）

当用户点击设置之后，会弹出一个模态框，用户可以分别设置不同模型的参数。模态框中包含以下内容：

#### 通用设置
- **AI 任务并发数**：最大可设置为 3，默认值为 1。用于控制批量生成执行时发往 AI 后端的并发阈值。

#### DeepSeek3.2
- max_tokens，默认值 2048
- temperature，默认值 0.7
- top_p，默认值 0.9
- frequency_penalty，默认值 0
- presence_penalty，默认值 0

#### Qwen3.5 Plus
- max_tokens，默认值 2048
- temperature，默认值 0.7
- top_p，默认值 0.9
- frequency_penalty，默认值 0
- presence_penalty，默认值 0

### 主体部分

#### 操作按钮

##### 导入
- 上传文件
    - 仅支持上传覆盖操作。**覆盖说明**：导入操作等同于“重建表格”。原有设置的表头列、列格式、所有 AI 字段配置将全部被清空，重置为从 csv 或 xlsx 文件中解析得到的字段。在导入前，弹出提示警告用户确认此清空操作。
- 上传文件格式
    - csv
    - excel（仅支持.xlsx 格式，若存在多个 Sheet 则仅读取首个 Sheet 的数据）
- 自动读取字段类型，如果无法解析字段类型，默认按文本处理
- **上传数据限制：最多允许上传 1000 条数据**，超过 1000 条数据时，会提示超出处理范围

##### 导出
- 下载文件
- 下载文件格式
    - csv
    - excel
- 下载数据范围
    - 全部
    - 选中数据

**关于数据行选中的交互说明**：
- 在表格的最左侧首列增加固定的**复选框列（Checkbox）**。
- 支持点击表头复选框进行“全选/反选当前由于筛选或本身存在的所有行”。
- 当有勾选时，导出的“选中数据”即基于这些勾选行导出，否则导出按钮默认为导出“全部”当前显示的行。

##### 筛选
- 筛选条件（根据当前表格的字段动态生成）
- 筛选方式（根据字段类型的不同，筛选方式也不同）
    - 文本
        - 等于
        - 不等于
        - 包含
        - 不包含
        - 为空
        - 不为空
    - 数值
        - 等于
        - 不等于
        - 大于
        - 小于
        - 大于等于
        - 小于等于
    - 日期时间
        - 等于
        - 不等于
        - 大于
        - 小于
        - 大于等于
        - 小于等于
- 筛选值
    - 文本 → 输入框
    - 数值 → 输入框
    - 日期时间 → 日期时间选择器
- 筛选逻辑
    - 与
    - 或

**筛选行为说明**：
- 筛选仅在页面中进行简单 filter，不涉及批量触发生成
- 即使在 AI 生成过程中亦可筛选，筛选行为不影响生成过程
- **无分页功能**（因数据仅在内存中，刷新即丢失）

#### 表格

表格包含以下内容：

- 动态列（默认 5 列：ID, Name, Gender, content, created_at）
- 每行数据可编辑
- **分页器：无**（数据量限制 1000 条，结合前端**虚拟滚动**技术全部展示）

##### 新增行与列
- **新增一列**：在所有列的最后，有一个可见的 "+" 列（仅有表头，没有数据），点击它弹出模态框以进行新增列的属性设定。
    - **内部映射提示（Column ID）**：在新建列的那一瞬间，系统必须为该列产生一个无法篡改的 uuid 即 Column ID（前端不可见）。用户在表头随时可以修改“字段名称”，但所有的前后端引用存储逻辑（包括 prompt {{引用}} ）底层必须与 Column ID 挂钩绑定，只在最后渲染态翻译为对应的字段名称展示。
- **新增一行**：在表格底部固定提供一行可见的“+”按钮，点击即可在末尾新增一层空行。
    - **新增行的自动化边界**：手动新增一行后，这一行均为“空数据”状态。它**不会立即自动触发**该行相关的 AI 字段解析，必须要经过用户点击单点触发 或 下达“生成 N 条”（往下探及覆盖到了此行）/“生成整列”等明确行为指令才会被执行。

##### 表头操作
用户点击表头可以对该字段进行编辑，编辑内容包括：
- 字段名称
- 字段类型
- AI 模型（对于 AI 字段类型）
- AI 模型输入/prompt（对于 AI 字段类型）
- 生成模式（对于 AI 字段类型）

**拖拽调整列顺序**：用户可以通过拖拽表头来调整列的顺序。

##### 列名修改与删除保护
- **更名联动**：当修改列的“字段名称”时，若是该字段被其他 AI 字段引用，系统底层应通过 Column ID 来维护其映射关系。针对 AI 字段内 prompt 文本中显式的 `{{旧字段名}}`，将其自动全部替换为 `{{新字段名}}`，以免引用链断裂出现报错。
- **删除保护**：当用户尝试删除一个被其他 AI 字段引用的字段时：
1. 弹出警告提示，明确告知用户该字段被哪些 AI 字段引用
2. 提示用户：删除后，引用该字段的 AI 字段的 prompt 将被自动修改（移除引用关系）
3. 用户确认后，执行删除操作，并自动清理所有引用关系

#### AI 字段生成

当用户为 AI 字段输入完 prompt 之后，可以通过以下几种交互方式触发生成：

- **单点生成/重试**
    - 针对单个空单元格，或曾经生成失败的单元格，提供轻量级的“✨ 生成”或“重试”按钮，允许用户点击后单独触发此单元格生成。
- **生成 [N] 条**（默认 N=10，数字可更改）
    - 从当前第一行空数据开始，连续生成 N 行
    - 如果中途有数据，则跳过已有数据的行
- **生成整列**
    - 遍历该列所有空数据的行，逐行生成

**生成状态显示与控制**：
- 生成过程中，单元格会显示加载状态（如旋转图标）。
- **编辑锁定**：处于“正在生成中”加载态的单元格将被暂时禁用，不再允许进行人工编辑录入，防止由于异步回调覆盖用户的输入。
- 生成完成后，显示生成的内容
- **生成失败时**：
    - 在单元格内清晰显示错误信息（如红色文字 + 错误图标）
    - 错误信息包含：失败原因（如 API 调用失败、token 超限等）
    - 提供"重试"按钮，用户可以点击重试该单元格的生成

## 错误处理

### AI 调用失败
- 错误信息必须清晰展示在页面中（单元格内或顶部通知栏）
- 错误类型包括：
    - API Key 配置错误
    - 网络错误
    - Token 超限
    - 模型服务不可用
    - Prompt 格式错误
- 每个错误都应提供明确的解决建议

### 数据校验
- 上传文件格式错误时，提示支持的格式
- 上传数据超过 1000 条时，提示超出限制
- 字段名称重复时，提示冲突

## 边界情况处理

### 数据丢失拦截与恢复建议
- **刷新/关闭前警告**：因为架构设定上“页面刷新即丢失，不涉及持久化”，为了防止用户因手误关闭 Tab 或进行跳转造成数小时的心血结晶遗失：
    - 监听 `beforeunload` 浏览器全局事件，只要当前表格中数据行数 > 0 且未发生过近期的导出行为，在用户尝试关闭标签页或刷新时，必然弹窗阻拦警告“系统发现您可能有未保存的数据，离开当前页面数据将丢失，是否确认离开？”。

### 循环引用检测
- 当 AI 字段 A 的 prompt 引用了 AI 字段 B，而 AI 字段 B 又引用了 AI 字段 A 时，将导致死锁。
- **检测时机**：**拦截在保存修改之前**。在用户编辑设定完 AI 弹窗 Prompt 点击“确认”保存的瞬间，同步校验映射树 graph 中是否形成闭环。若出现，则直接在该设定表单抛出 Error 拦截保存。
- 建议用户先手动填写其中一个字段，或修改引用关系

### 字段引用语法
- 使用 `{{field_name}}` 语法
- 在 prompt 中，`{{field_name}}` 会被替换为当前行对应字段的值
- **引用的内容为空**：视为普通的文本拼接处理，如果引用的目标单元格恰好为空，则它会被替换为主动拼接的空字符串，该行的生成任务可以正常继续下发。
- **引用的字段不存在**：如果引用的字段/列名由于存在拼写错误、亦或已被删除，此时应拦截针对这一行/该单元格的生成请求，在生成前或单元格内给予红字报错，提示缺少相应的字段引用。

### 大数据量性能
- 数据上限 1000 条，全部在内存中处理，结合前端的**虚拟滚动**确保视图流畅度。
- AI 批量生成时，采用任务队列结合并发池的控制方式，依据系统设置中的“AI 任务并发数”派发请求，以避开大范围请求瞬间拉跨页面的同时提升并发响应效率。
- 生成过程中用户可以继续操作其他功能（如筛选、编辑其他列）

## 安全与配置

- **API Key 管理**：配置在服务器 `.env` 文件中，前端不可见
- **权限管理**：内部使用，暂不做用户权限系统
- **数据隐私**：数据仅在内存中，刷新即丢失，不存储到任何持久化介质

---

## 版本记录

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| 1.0 | 2026-02-25 | 初始版本 |
| 1.1 | 2026-02-25 | 补充技术栈、AI 触发机制、错误处理、边界情况等详细说明 |
| 1.2 | 2026-02-25 | 结合 Review 反馈补充了：虚拟滚动、并发控制、修改列名联动、编辑锁定以及导入提示和空值控制相关的机制 |
| 1.3 | 2026-02-25 | 细化补全了基于底层 Column ID 的机制定义、前端渲染组件选型参考、行与导出的 Checkbox 复选联动方式、循环引用的拦截保存时机、以及刷新/关闭浏览器前的拦截挽回提示。 |
