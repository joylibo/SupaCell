# SupaCell需求文档


SupaCell是一个web项目，它看上去简洁、优雅，页面功能清晰，易于理解和使用。

其核心功能是一个可编辑的在线表格，默认表格有5列，分别是：

- ID
- Name
- Gender
- content
- created_at

在所有列的最后，有一个“+”列（仅有表头，没有数据），点击它会弹出一个模态框，模态框中包含以下表单：

- 字段名称
- 字段类型
    - 文本
        - 默认值
    - 数值
        - 默认值
    - 日期时间
        - 默认值
    - 单选
        - 选项
    - 多选
        - 选项
    - AI
        - 模型
            - DeepSeek3.2
            - Qwen3.5 Plus
        - prompt
            - prompt的输入框是一个textarea, 其右上角有一个“引用字段”按钮，点击它会弹出一个模态框，模态框中包含一个字段列表（列表顶部有一个搜索框，用户可以搜索字段），用户可以选择字段，选择之后，该字段的引用会自动添加到prompt中


用户可以添加、删除、修改表格中的数据，也可以修改表格的列，包括列名称、列类型、列顺序等。

然而，与一般的可编辑在线表格不同的是，用户在supacell中新增一个列的时候，除了选择常规的字段类型（数值、文本、日期时间、单选、多选）之外，还可以选择AI字段类型。

AI字段类型的数据，是通过调用AI模型来生成的，用户可以设置AI模型的参数，比如：

- AI模型（可选DeepSeek3.2或Qwen3.5 Plus）
- AI模型的输入（用户可以设置AI模型的输入，比如：prompt）


## 页面布局

### 顶部栏

顶部栏包含以下内容：

项目名称（SupaCell），设置入口（齿轮图标+文字）

当用户点击设置之后，会弹出一个模态框，用户可以分别设置不同模型的参数，模态框中包含以下内容：

- DeepSeek3.2
    - max_tokens，默认值2048
    - temperature，默认值0.7
    - top_p，默认值0.9
    - frequency_penalty，默认值0
    - presence_penalty，默认值0

- Qwen3.5 Plus
    - max_tokens，默认值2048
    - temperature，默认值0.7
    - top_p，默认值0.9
    - frequency_penalty，默认值0
    - presence_penalty，默认值0

### 主体部分

主体部分包含以下内容：

#### 操作按钮

- 导入
    - 上传文件（默认只有上传覆盖这一种形式，即上传之后页面中的所有内容都会被覆盖）
    - 上传文件格式
        - csv
        - excel（仅支持.xlsx格式）
    - 自动读取字段类型，如果无法解析字段类型，默认按文本处理
    - 上传数据限制：最多允许上传5000条数据，超过5000条数据时，会提示超出处理范围。

- 导出
    - 下载文件
    - 下载文件格式
        - csv
        - excel
    - 下载数据范围
        - 全部
        - 选中  

- 筛选
    - 筛选条件
        - ID
        - Name
        - Gender
        - content
        - created_at
    - 筛选方式（根据字段类型的不同，筛选方式也不同）
        - 文本
            - 等于
            - 不等于
            - 包含
            - 不包含
            - 为空
            - 不为空
        - 数值
            - 等于
            - 不等于
            - 大于
            - 小于
            - 大于等于
            - 小于等于
        - 日期时间
            - 等于
            - 不等于
            - 大于
            - 小于
            - 大于等于
            - 小于等于
    - 筛选值
        - 文本
            - 输入框
        - 数值
            - 输入框
        - 日期时间
            - 日期时间选择器
    - 筛选逻辑
        - 与
        - 或

#### 表格

表格包含以下内容：

- ID
- Name
- Gender
- content
- created_at

分页器，每页显示100条数据。

#### 表头
用户点击表头可以对该字段进行编辑，编辑内容包括：
- 字段名称
- AI模型（对于AI字段类型）
- AI模型输入（对于AI字段类型）
